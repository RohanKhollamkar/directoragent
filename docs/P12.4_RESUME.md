# P12.4 — first real generation (agent-mediated) — RESUME NOTE

**Status:** blocked only on connector attachment. Code through STEP 12.3 is on
`main`. **No credits have been spent.** We are at the pre-spend gate (before
step 1's `get_cost` even).

## Why this note exists
The first real generation is driven **agent-mediated**: the assistant calls the
`mcp__Higgsfield__*` tools directly as the transport (NOT via a production MCP
client — that A/B transport decision is deferred, see P12 notes). During the
attempt, this remote session lost the Higgsfield connector and would not
re-attach it mid-session (`ListConnectors` kept reporting
`enabledInChat: false` and no tools loaded, despite the chat-UI toggle being
on). **Fix: run P12.4 in a FRESH Claude Code session with Higgsfield enabled at
startup** — a new session binds its MCP connectors on init (that's how the
STEP 12.3 live smoke worked). The real "tools are live" signal is a `ToolSearch`
for "Higgsfield" returning tools, not the UI toggle.

## Do NOT change the transport
This exercise uses agent-mediated MCP calls only. Do not wire a production MCP
client / REST transport (deferred A/B decision). The point is to drive ONE real
job through the adapter's **param-building + parsing logic**, using the live MCP
tools as the hands.

## Spend gate (hard rule)
STOP after step 1 (`get_cost`) and wait for explicit "go" before step 2. Only
ONE real job total. Use the cheapest shot (Wan 2.6, 5s).

---

## Step 1 — cost preflight (NON-SPENDING). Stop and report the credit figure.
Params are built by the REAL adapter code path
(`HiggsfieldClient._generate_params` + `_prompt_with_motion`) for a Wan 2.6 / 5s
shot. Verified they render exactly as:

```json
{
  "model": "wan2_6",
  "prompt": "Abstract ink dispersing in water, deep indigo and cyan tendrils unfurling, soft volumetric light. Camera: locked-off static camera.",
  "duration": 5,
  "aspect_ratio": "16:9",
  "get_cost": true
}
```

The Shot used to build them:
- `model=Model.WAN_2_6` (catalog id `wan2_6`), `render_class=ABSTRACT_FLUID`
- `duration_s=5`, `motion_preset="STATIC"` (folds to "locked-off static camera")
- `prompt="Abstract ink dispersing in water, deep indigo and cyan tendrils unfurling, soft volumetric light."`
- `reference.source="assets/test.png"`

Action: call `mcp__Higgsfield__generate_video` with the JSON above (get_cost=true,
NO media — preflight is media-independent, verified veo3_1@8s=22 earlier).
Report `{"cost":{"credits":N,...}}`. **STOP — wait for go.**

## Step 2 — on GO: media handshake + ONE real job
- Run the adapter's `_resolve_media("assets/test.png")` logic. `assets/test.png`
  is a local path → `media_upload(filename=...)` → PUT bytes to the returned
  presigned `upload_url` → `media_confirm(media_id, type="image")`. Capture the
  `media_id`. (If easier to drive by hand: reproduce that exact sequence via the
  MCP tools; confirm the real `media_upload` RESPONSE shape and update
  `_upload_local`'s defensive parsing + the `TODO(P12.4)` there.)
- Build the SUBMIT params via `_generate_params(shot, media_id)` — same as step 1
  but WITH `medias=[{"value": media_id, "role": "image_references"}]` (Wan's
  start-image role is `image_references`, not `start_image`) and WITHOUT
  `get_cost`.
- Call `mcp__Higgsfield__generate_video` for real (ONE job). Capture the returned
  **job_id** (UUID).

## Step 3 — poll to terminal; capture RAW statuses
- Poll `mcp__Higgsfield__job_display(id=job_id)` repeatedly to a terminal state.
- Record the **raw status string at EVERY poll** (verbatim) — this resolves the
  `TODO(P12.4)` in `clients/higgsfield.py` `poll()` and the status vocab TODO in
  `_upload_local`/status sets.
- Capture the final **result asset URL** (`fetch_result` logic:
  `job_display` → `results[0].url`) and the **credit spend** (compare `balance`
  before/after, or read the generation's cost).

## Step 4 — report
- Map each raw status to the adapter's normalization sets
  (`_SUCCEEDED` / `_FAILED` / `_RUNNING`) and state whether ANY raw status hit
  the defensive `unknown -> WARNING + "running"` branch.
- Then update `clients/higgsfield.py`: tighten `_SUCCEEDED/_FAILED/_RUNNING` to
  the real vocabulary, fix `_START_IMAGE_ROLE`/response-shape parsing if the live
  shapes differ, and remove/update the `TODO(P12.4)` markers. Commit + push.

---

## Confirmed-live facts (from STEP 12 discovery + 12.3 smoke, this project)
- Catalog IDs exist: `seedance_2_0`, `kling3_0`, `wan2_6`, `veo3_1`.
- Start-image role per model: `start_image` for Seedance/Kling/Veo;
  **`image_references` for Wan** (no `start_image` role).
- `get_cost` is a no-spend preflight; response shape
  `{"cost":{"credits":N,"credits_exact":N}}`. Real: veo3_1@8s = **22 credits**.
- API has **no idempotency key**; `reconcile` is a best-effort {model,prompt}
  fingerprint over `show_generations`.
- Account balance at 12.3 discovery: 100 credits, "plus" plan.
- STILL UNKNOWN (this note resolves): exact `job_display`/`show_generations`
  status STRINGS, and the `media_upload` response shape (presigned url + id keys).

---

## RESOLVED — live run record (P12.4 executed 2026-07-06)

- **get_cost** (wan2_6 @ 5s, adapter-built params): `{"cost":{"credits":13,"credits_exact":13}}`.
- **Job:** `58c50606-ee3a-46cd-8904-8e4e99bd0298`. **Spend:** balance 100 → 87
  (= 13 credits, matches preflight exactly).
- **Raw statuses observed:** `"pending"` (submit response) → `"in_progress"`
  (poll) → `"completed"` (terminal). All three MISSED the old parsing — not
  because of vocabulary, but because `generate_video`/`job_display` wrap the
  job in a `{"results":[{...}]}` envelope the adapter didn't unwrap, so every
  poll would have hit the defensive `unknown -> running` branch (raw `None`).
  Fixed: `_job_entry()` unwraps the envelope; status sets tightened to the
  real vocabulary (failure strings still unobserved — success-only run).
- **Result URL** (at `results[0].results.rawUrl` — a dict, not a list):
  `https://d8j0ntlcm91z4.cloudfront.net/user_3FdggLn5IvFD0ORQbHZhSgip7Mf/hf_20260706_160230_58c50606-ee3a-46cd-8904-8e4e99bd0298.mp4`
- **media_upload response shape:** `{"uploads":[{"upload_url", "media_id",
  "url" (cdn), "content_type", "expires_in_seconds", "method":"PUT", ...}]}`.
  The presigned URL signs `content-type`, so the PUT must send the matching
  `Content-Type` header (`_put_bytes` now does).
- **Handshake deviation:** the sandbox egress proxy policy-denied the direct
  PUT to `upload.higgsfield.ai`, so the media went through `_resolve_media`'s
  OTHER real branch — `media_import_url` on the raw GitHub URL of
  `assets/test.png` (committed to the repo). Its response shape
  `{"media_id", "type", "content_type", "source_url"}` parsed cleanly.
- **Transient errors are real:** `get_cost` failed once and `media_upload`
  three times with server-side "Something went wrong" before succeeding
  unchanged — the tenacity seam on the MCP boundary is earning its keep.
